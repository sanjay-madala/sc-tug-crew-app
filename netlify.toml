[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — every route must serve index.html so React Router takes over
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
